# Used only during Docker image build to pre-download Vale style packages.
# Content repos (e.g. architecture-docs) have their own .vale.ini at runtime.
StylesPath = .vale/styles
MinAlertLevel = suggestion
Packages = RedHat, write-good
